# AI Agent Company System (AI 员工管理系统)

这是一个基于 **Python + FastAPI + Streamlit** 构建的 AI Agent 编排与管理平台。
它允许你像经营公司一样，招聘不同角色的 AI 员工 (Agents)，为它们定义人设 (System Prompts)，并下达任务 (Tasks) 产出实际的工作成果。

## 🎯 核心设计理念

本项目旨在探索 **"AI Agent as a Workforce" (AI 即劳动力)** 的概念：
1.  **拟人化管理**: 每个 Agent 都有独立的“档案”，包含姓名、职位、性格 (Prompt) 和技能。
2.  **异步任务流**: 任务的下达与执行是解耦的。你派单，Agent 接单并后台执行，最终交付文件。
3.  **结果导向**: 强调“产出物”。无论 Agent 做什么，最终都要在这个系统中生成一个可用的文件 (Markdown/代码/报告)。
4.  **轻量级架构**: 使用 SQLite + Streamlit，无需复杂的服务器部署，单机即可运行一个“微型跨国公司”。

## 📂 项目结构

```
/Company System
├── /backend                 # 后端服务 (FastAPI)
│   ├── /app
│   │   ├── main.py          # API 核心路由与逻辑
│   │   ├── models.py        # 数据库模型 (Agent, Task, Settings)
│   │   ├── schemas.py       # Pydantic 数据验证
│   │   ├── crud.py          # 数据库增删改查
│   │   └── database.py      # SQLite 连接管理
│   └── requirements.txt     # 后端依赖
│
├── frontend_app.py          # 前端界面 (Streamlit)
├── launcher.py              # 统一启动器 (多进程管理)
├── run_system.bat           # Windows 一键启动脚本
├── stop_system.bat          # 停止脚本 (清理进程)
└── /outputs                 # [自动生成] Agent 任务产出的文件存放区
```

## 🚀 快速开始

### 1. 环境要求
*   Windows 10/11
*   Python 3.8+

### 2. 启动系统
双击项目根目录下的 **`run_system.bat`**。
*   脚本会自动创建虚拟环境 (`venv`)。
*   自动安装依赖。
*   自动启动后端 API 和前端网页。

### 3. 系统配置
*   首次进入系统，请访问左侧菜单的 **Settings**。
*   填入你的 **API Key** (支持 OpenAI, DeepSeek, Moonshot 等兼容 OpenAI 格式的 API)。
*   填入 **Base URL** (如 `https://api.openai.com/v1`)。

## 🛠️ 技术栈

*   **Backend**: FastAPI, SQLAlchemy, SQLite
*   **Frontend**: Streamlit
*   **AI Integration**: Requests (Direct HTTP calls to avoid dependency hell)
*   **Process Management**: Python `subprocess`

## 📝 许可证
MIT License
